배열이란?
	- 동일한 타입의 데이터가 여러개 저장되어 있는 메모리의 저장장소

	int number;
	int number1;
	int number2;

    //정수를 담는 배열 3개 선언
	//int number[3];
	//배열 선언 -> 배열 공간의 갯수를 만들어 준다.

for문을 사용하여  동일한 코드로 배열공간에 넣어줄수 있다.
    //for (int i = 0; i < 3; i++)
	//{
	//	num[i] = i;
	//	//1번째 num[0] = 0;
	//	//2번째 num[1] = 1;
	//	//3번쨰 num[2] = 2;
	//}

    	/*
	배열을 쓸때 주의할 점!!
	- 배열을 선언 후 값을 초기화(대입) 해주지 않으면 
	쓰레기 값이 들어간다.
	따라서 반드시 배열을 선언 후에는 값을 초기화 해줘야 한다.
	*/